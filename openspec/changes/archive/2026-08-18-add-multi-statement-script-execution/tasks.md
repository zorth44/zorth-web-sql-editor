## 1. 切分与脚本模型基础

- [x] 1.1 在 `web/src/sql-editor/sql.ts` 增加脚本切分入口，返回语句列表并标示切分是否可靠（扫描结束时状态机未回到 normal 视为不可靠）
- [x] 1.2 增加会话状态语句检测函数，识别 `SET`、`CREATE TEMPORARY TABLE`、`USE` 开头的语句（正确跳过前置注释与空白）
- [x] 1.3 定义脚本语句数量上限常量（默认 200）并集中导出
- [x] 1.4 为 1.1–1.3 补充 `web/src/sql-editor/sql.test.ts` 用例：引号/反引号/`#`/`--`/块注释中的分号、未闭合引号退化、会话语句识别、空语句剔除
- [x] 1.5 核对前端切分用例与 `service/src/test/java/com/bocsoft/sqleditor/execution/SqlStatementScannerTest.java` 保持对等

## 2. 编辑器 store 的多结果模型

- [x] 2.1 在 `web/src/stores/editor.ts` 将 `EditorTab` 的单结果字段扩展为语句结果集合（序号、SQL、状态、结果、错误），并保留当前查看索引
- [x] 2.2 让单条执行退化为集合长度为 1 的情形，保证单条与脚本共用同一渲染路径
- [x] 2.3 调整 `start`/`finish`/`abort`，支持脚本级运行状态、逐条写入结果、以及停止时丢弃待执行队列
- [x] 2.4 确认 `persist()` 仍只写入 SQL 草稿与连接标识，不把结果写进 Session Storage
- [x] 2.5 补充 store 单测：脚本逐条推进、遇错即停、停止后队列清空、页签级重复提交仍被拒绝

## 3. 编辑器组件与运行入口

- [x] 3.1 在 `web/src/components/editor/SqlMonacoEditor.vue` 上抛选区存在状态，供按钮文案切换
- [x] 3.2 新增取「选区全文或编辑器全文」的脚本文本入口，与现有 `getRunnableStatement` 并存
- [x] 3.3 将 `Cmd/Ctrl + Shift + Enter` 改为脚本执行，移除“暂不支持批量执行”提示
- [x] 3.4 保留 `Cmd/Ctrl + Enter` 为运行当前语句（选区单条或光标所在语句）
- [x] 3.5 在 `web/src/pages/SqlEditorPage.vue` 让运行按钮在有选区时显示“运行选中”、无选区时显示“运行”，并更新按钮 title 与快捷键提示

## 4. 脚本串行编排

- [x] 4.1 在 `SqlEditorPage.vue` 实现脚本编排：切分、超限拒绝、会话状态警告、逐条串行调用现有执行接口
- [x] 4.2 每条语句生成独立 UUID，沿用现有连接校验（未选数据源拦截、`likelyNeedsDatabase` 判断）
- [x] 4.3 实现遇错即停：失败后不再发起后续语句，保留已完成结果并记录失败序号
- [x] 4.4 实现停止语义：取消在飞语句 + 清空待执行队列，恢复可运行 UI
- [x] 4.5 脚本执行期间整个页签视为一个运行中执行，仅占用 1 个并发额度
- [x] 4.6 脚本中出现 DDL 成功时仍触发元数据失效与刷新
- [x] 4.7 页面卸载时中止在飞请求并丢弃队列

## 5. 结果区多结果呈现

- [x] 5.1 新增结果容器组件，承载 Statistics 汇总页签与按语句序号排列的 `Result N` 子页签
- [x] 5.2 子页签内复用现有 `ResultGrid`，保持选区复制、值面板、排序筛选固定、CSV 导出等既有能力
- [x] 5.3 Statistics 汇总展示每条语句的序号、类型、状态、耗时、返回或影响行数，并标注失败语句
- [x] 5.4 汇总面板中明确说明遇错停止后已执行语句不会回滚
- [x] 5.5 脚本运行中展示进度（第 i / N 条）
- [x] 5.6 单条执行时不显示多余的页签壳层，保持与当前视觉一致

## 6. 测试

- [x] 6.1 组件测试：按钮文案随选区切换、快捷键分别触发单条与脚本
- [x] 6.2 组件测试：脚本串行执行顺序、进度展示、多结果子页签渲染与切换
- [x] 6.3 组件测试：脚本中途失败后停止、保留已完成结果、标注失败语句、不可回滚提示
- [x] 6.4 组件测试：会话状态语句警告、语句数超限拒绝、切分不可靠时整段发送
- [x] 6.5 组件测试：脚本执行中点击停止后队列清空且 UI 恢复可运行
- [x] 6.6 E2E：粘贴多条语句脚本并运行，逐条查看结果与汇总
- [x] 6.7 跑通 `pnpm` 全量校验（格式化/lint、类型检查、Vitest、Playwright、生产构建）

## 7. 文档与规格收尾

- [x] 7.1 更新 `docs/frontend-development-spec.md` 的 7.4 Monaco 功能（快捷键语义、移除批量执行拦截）
- [x] 7.2 更新 `docs/frontend-development-spec.md` 的 7.5 执行交互（脚本串行、遇错即停、停止语义、会话状态限制）
- [x] 7.3 更新 `docs/frontend-development-spec.md` 的 7.6 查询结果（多结果子页签与汇总）
- [x] 7.4 确认 `docs/backend-development-spec.md` 与后端规格无需改动，`MULTI_STATEMENT_NOT_SUPPORTED` 仍作为权威兜底
- [x] 7.5 在发布说明中点出破坏性交互变更：运行按钮无选区时由“执行当前语句”变为“执行全部语句”
- [x] 7.6 运行 `openspec validate --all` 并归档本 change
